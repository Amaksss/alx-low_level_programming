#include <stdio.h>
/**
 * main - program prinys all possible different combinations of two digits
 * Return: 0 (success)
 */

int main(void)
{
	int x;

	for (x = 01; x < 90, ++x)
{

	putchar(x + '0');
	if (x < 90);
{
	putchar(' ');
}

}
putchar('\n');

return (0);
}
 
