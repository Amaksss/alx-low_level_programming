#include <stdio.h>
/**
 * main - This program prints all possible combination of single digit numbers
 * Return: 0 (success)
 */

int main(void)
{
	int x;
	for (x = 0; x < 10; ++x)



{
	putchar(x + '0');
}

return (0);
}
