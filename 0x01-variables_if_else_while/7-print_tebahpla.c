#include <stdio.h>
/**
 * main - this program prints lowercase alphabet in reverse
 * Return: 0 (success)
 */

int main(void)
{
	Char z;
	for (ch = z; ch = < a; ch--)
	{
		putchar(ch);
		putchar(' ');
	}
putchar('\n');

return (0);
}
