#include <stdio.h>
/**
 * main - this program prints lowercase alphabet in reverse
 * Return: 0 (success)
 */

int main(void)
{
	char i;
	for (i = z; i >= a; i--)
	{
		putchar(i);
		putchar(' ');
	}
putchar('\n');

return (0);
}
