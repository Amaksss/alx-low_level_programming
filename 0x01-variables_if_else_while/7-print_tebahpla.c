#include <stdio.h>
/**
 * main - this program prints lowercase alphabet in reverse
 * Return: 0 (success)
 */

int main(void)
{
	int x;
	for (x = z; x =< a; x--)
	{
		putchar(x + 'a');
		putchar(' ');
	}
putchar('\n');

return (0);
}
