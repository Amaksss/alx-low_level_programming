#include "main.h"
#include <stdio.h>

/**
 * main - Entry point
 * @c: parameter
 * Return: Always 0
 */

int _isupper(int c)
{
	char c;

	if (c = 'a'; c <= 'z'; c++)
	{
		return(1);
	}
	else
	{
		return(0);
	}
}
